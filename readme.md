# simple java 25

just some Java-learning stuff

```sh
# compile and run
make run
```

## dependencies

You need a Java 25 JDK.

```sh
# e.g.
brew install openjdk@25 # mac
choco install openjdk # windows
```

You need `make`.

```sh
# e.g.
brew install make # mac
choco install make # windows
```
